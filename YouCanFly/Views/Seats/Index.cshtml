@model IEnumerable<YouCanFly.Models.Seats>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Color)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SeatsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SeatsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SeatsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
